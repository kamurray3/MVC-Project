@model IEnumerable<Murray_P1.Models.PossibleState>

@{
    ViewBag.Title = "ChooseState";
}

<h2>ChooseState</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table">
    <tr>
        <th>
            View Customers in State
            @*@Html.DisplayNameFor(model => model.FullStateName)*@
        </th>
        <th></th>
    </tr>

    @foreach (var stateObject in Model)
    {
        <tr>
            <td>
                <b>
                    @Html.ActionLink(stateObject.FullStateName,
                                     "ByState",
                                     "JordanCustomers",
                                     new { id = stateObject.StateAbbr},
                                     null)
                </b>
            </td>
            @*<td>@customerObject.CustomerState </td>
               *@
        </tr>

        @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StateAbbr)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.CustomerEmail }) |
                    @Html.ActionLink("Details", "Details", new { id=item.CustomerEmail }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.CustomerEmail })
                </td>
            </tr>*@
    }

</table>